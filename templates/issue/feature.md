---
name: Feature request
about: Propose new functionality, following the SDD workflow
labels: enhancement
---

## Summary

One or two sentences: what this adds and why.

## Motivation

What problem does this solve? What happens if it is not built?

## Spec

Link the spec document for this feature (`docs/<feature>.md` or
`docs/features/<feature>.md`), or inline a short spec here if the change is
small enough that a standalone doc would be overkill: see
`workflow/sdd.md` for rigor levels.

## Acceptance criteria

Numbered, testable, EARS-style where possible ("When `<trigger>`, the
system shall `<behavior>`"). Assign a traceability id per criterion,
`FR-<DOMAIN>-<NN>` or `AC-<AREA>-<NN>` (see `workflow/sdd.md`):

- [ ] `<ID>`:
- [ ] `<ID>`:

## Proposed approach

Enough of the design to catch disagreement before implementation starts.
It does not need to be the final design.

## Out of scope

What this explicitly does not cover, so reviewers do not expect it.

## References

Related issues, prior art, design docs.
